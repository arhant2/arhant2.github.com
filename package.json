{
  "name": "base",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "live-server",
    "start:watch:scss": "node-sass -w -r ./scss -o ./public/css",
    "start": "run-p start:server start:watch:scss",
    "build": "node-sass ./scss -o ./public/css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.1",
    "node-sass": "^6.0.0",
    "npm-run-all": "^4.1.5"
  }
}
